cars = []

addName = True
while addName == True:
  newCar = str(input("enter a new car or x to exit >>> "))
  if newCar == "x":
    print("now exiting...")
    addName = False
  else: 
    cars.append(newCar)
  
print("in the list =" ,cars) 
